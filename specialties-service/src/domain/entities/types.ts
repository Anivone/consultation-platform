export interface SpecialtyProps {
    id?: string;
    version: number;
    name: string;
    sphereId: string;
}

export interface SphereProps {
    id?: string;
    version: number;
    name: string;
    tags: string[];
}
